//*exercise #1
/** result = prompt(title, [default]);
 title
    El texto a mostrar al usuario.
 default
    Un segundo parámetro opcional, es el valor inicial del campo de entrada.*/

/**
 let flag = 0;
do {
    
    /**let a = prompt('Are you beautiful? (yes/no)');

    if (a === 'yes') {
        alert("Of course!");
        flag = 1;

    }
    else if (a === 'no'){
        alert("I don't believe you");
        flag = 1;
    }
    else {
        alert("Write a correct input");
    }*/

    /**let a = confirm('Are you beautiful?');

    if (a == true) {
        alert("Of course!");
        flag = 1;

    }
    else if (a == false){
        alert("I don't believe you");
        flag = 1;
    }
    else {
        alert("Write a correct input");
    }

} while (flag == 0);
 */

//* print numbers only
/**
 let array = [4, "dos", 8, "tres", 5, 9, 1, "cero"];

for (let i = 0; i < array.length; i++){
    if (!isNaN(array[i])){
        console.log(array[i]);
    }
    //another way
    //if(typeof array[i] === "number"){
    //console.log(array[i]);}
}*/

//*exercise #2